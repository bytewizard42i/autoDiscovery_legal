VITE_CONTRACT_ADDRESS=""

# AD mode: "demoland" uses mock providers; "realdeal" uses live blockchain providers
VITE_AD_MODE=demoland

# Midnight preprod indexer GraphQL endpoint (used in realdeal mode)
VITE_INDEXER_URL=

# Deployed discovery-core contract address (used in realdeal mode)
VITE_CONTRACT_DISCOVERY_CORE=
